import { Inter, Lusitana, Oranienbaum } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'] });
export const oranienbaum = Oranienbaum({
    subsets: ['latin'],
    weight: ['400'],
})